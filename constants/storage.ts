/** AsyncStorage key for persisted snooze timestamps. */
export const SNOOZE_STORAGE_KEY = 'meditrack:snoozedUntil';

/** AsyncStorage key for dismissing the battery optimization modal. */
export const BATTERY_OPT_DISMISSED_KEY = 'meditrack:batteryOptDismissed';

/** AsyncStorage key for per-placement ad preferences. */
export const AD_PREFERENCES_KEY = 'meditrack:adPreferences';
